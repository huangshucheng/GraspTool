>支持本地存储玩家openid, unionid等必要信息，存到本地，下次直接读取，不用再次点开微信活动页面：暂时先支持读写token,不区分玩家 OK

> 列出微信名字，方便查看列表和请求情况， 列表显示：cookie，结果，红包，状态（完成，执行中）
> 破解任务：考场，等等，任务形成统一规范
> 支持接码平台
> 脚本中文读取问题 (暂时部分中文还不能及时显示)
> 破解token, 用openid + unionid动态生成
> 发布在服务器上 TODO
> 可手动输入token

TODO---1228
> 支持做指定那条Cookie的活动任务 TODO
> 支持：多线程请求http，线程数，请求次数
> 完善代理功能：输入代理IP及端口,  IP有效性检测
> 将token输出页面改成表格，列：序号，Cookie, 结果（是否有红包，有多少）， 状态（完成，进行中）
> 定时器，做定时任务
> 本地IP显示

> Cookie 查找，要找到全部所需内容，才算查找成功 OK
> 每个活动支持自定义配置请求头OK
> 任务执行封装 OK 
> 支持脚本配置任务 OK
> 请求，支持延时xx秒，做选项 OK
> 支持，卡包次数：卡红包步骤的次数 OK
> 播放声音 OK
> 停止抓包功能 OK
> 自动开始 OK
> winForm UI事件到 lua的映射 OK
> 支持切换活动 OK